{{ form_start(form) }}
    <div class="input-group">
        {{ form_widget(form.query, { 'attr': { 'class': 'input-group-field' } }) }}
        <div class="input-group-button">
            <button type="submit" class="button">{{ lyssal_searchengine_submitvalue() }}</button>
        </div>
    </div>
    {{ form_rest(form.query) }}
{{ form_end(form) }}

